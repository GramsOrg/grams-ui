import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: 'Grams UI',
  brandUrl: 'https://gramsorg.github.io/grams-ui/',
  brandTarget: '_self',
});
